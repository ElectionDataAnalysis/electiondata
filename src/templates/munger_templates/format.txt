item	value
header_row_count	<integer or None: number of rows at the top of the file not containing data>
field_name_row	<integer or None;0 is the top row. It is assumed that there is only one row with labels for fields (though there may be more than one header row with values, such as contest or candidate names)>
field_names_if_no_field_name_row	<None or a comma-separated list>
count_columns	<comma-separated list; 0 is the left-most column>
file_type	<csv, txt, xls or xlsx>
encoding	<if you're not sure, use iso-8859-1>
thousands_separator	<typically None or ,>
